{
  "name": "@descope-trust-center/api",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint --flag unstable_native_nodejs_ts_config",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@descope-trust-center/auth": "workspace:*",
    "@descope-trust-center/db": "workspace:*",
    "@descope-trust-center/validators": "workspace:*",
    "@trpc/server": "catalog:",
    "drizzle-orm": "^0.44.0",
    "superjson": "2.2.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@descope-trust-center/eslint-config": "workspace:*",
    "@descope-trust-center/prettier-config": "workspace:*",
    "@descope-trust-center/tsconfig": "workspace:*",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "prettier": "@descope-trust-center/prettier-config"
}
